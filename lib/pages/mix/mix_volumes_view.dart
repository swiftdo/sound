import 'package:flutter/material.dart';

class MixVolumesView extends StatelessWidget {
  const MixVolumesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
